set(TEST_CONTROLLER_LIB_NAME "test")

file(GLOB TEST_CONTROLLER_SRC
    *.hpp
    *.cpp)

add_library(${TEST_CONTROLLER_LIB_NAME} MODULE ${TEST_CONTROLLER_SRC})

target_link_libraries(${TEST_CONTROLLER_LIB_NAME} ${BASE_CONTROLLER_LIB_NAME})
